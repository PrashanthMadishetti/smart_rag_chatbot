# Embeddings / Vector store
HF_EMBED_MODEL=
VECTOR_BACKEND=
INDEX_DIR=

# Server
HOST=
PORT=
LOG_LEVEL=

# Redis Memory
REDIS_URL=

# Optional: expire conversation buffers after N seconds (omit or set 0 for no TTL)
MEMORY_TTL_SECONDS=

# LLM providers (free)
GEMINI_API_KEY=
GROQ_API_KEY=

# Optional: default model choices (override at runtime if you want)
GEMINI_MODEL=
GROQ_MODEL

DATABASE_URL=
JWT_SECRET=
JWT_ISSUER=
JWT_ALG=
JWT_EXPIRES_MIN=
ACCESS_TTL_SECONDS=
LOGIN_TTL_SECONDS=

# Password hashing
BCRYPT_ROUNDS=

# Vector storage
INDEX_DIR=


#Pinecone
VECTOR_BACKEND=
PINECONE_API_KEY=
PINECONE_REGION=
PINECONE_INDEX=
PINECONE_CLOUD=
