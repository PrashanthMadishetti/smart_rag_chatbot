# Auth
JWT_SECRET=change-me

# Embeddings / Vector store
HF_EMBED_MODEL=sentence-transformers/all-MiniLM-L6-v2
VECTOR_BACKEND=faiss
INDEX_DIR=/data/indexes/faiss   # inside the container

# Server
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# Redis Memory
REDIS_URL=redis://redis:6379/0

# Optional: expire conversation buffers after N seconds (omit or set 0 for no TTL)
MEMORY_TTL_SECONDS=0